import { IO, Input, Output } from 'rete';
import { Picker } from './picker';
export declare type FlowParams = {
    input?: Input;
    output?: Output;
};
export declare class Flow {
    private picker;
    private target?;
    constructor(picker: Picker);
    private act;
    start(params: FlowParams, io?: IO): void;
    complete(params?: FlowParams): void;
    hasTarget(): boolean;
    private unlock;
}
